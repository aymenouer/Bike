{% extends 'temp_admin.html.twig' %}
{% block content %}
    <div class="row">
        <div class="col-lg-12">

            <div class="row">
                <div class="col-lg-4 col-md-4 col-sm-4 mb" style="
    margin-left: 250px;
    margin-top: 30px;
">
                    <div class="product-panel-2 ">
                        <div class="badge badge-hot">New</div>
                        <img src="{{ asset('uploads/images/' ~ produit.image) }}" style="border-radius: 70px;" width="200" alt="">
                        <h5 class="mt">Libelle : {{produit.libelle}}</h5>
                        <h6>Prix : {{produit.prix}}DT</h6>
                        <h6>Categorie : {{produit.libC}}</h6>
                        <h6>Quantite : {{ quantite }}</h6>
                        <h6>Site : {{produit.libS}}</h6>
                        <h6 {% if etat == "Bonne etat" %} style="color: darkgreen" {% else %} style="color: red"  {% endif %}  >Etat :   {{etat}}  {% if etat == "Bonne etat" %} 👍 {% else %} 👎 {% endif %} </h6>

                        <h6>Description : {{produit.description}}</h6>


                        <button class="btn btn-small btn-theme04">    <a href="{{ path('_list_piece')}}"   style="color: white;" ><- Back</a> </button>
                    </div>
                </div>


            </div>

        </div>
    </div>

{% endblock %}